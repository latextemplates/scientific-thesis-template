version: 2
jobs:
   build:
     docker:
       - image: koppor/texlive
     steps:
       - checkout
       - run: latexmk main-german.tex
       - run: latexmk main-english.tex
       - run: latexmk main-paderborn-english.tex
       - run: mkdir build && cp README.md main-*.pdf build/
       #- run: .circleci/deploy-ghpages.sh build/
